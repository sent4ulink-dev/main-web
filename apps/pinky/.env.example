# Frontend build variable (Cloudflare Pages). The Worker's real deployed address —
# see worker/wrangler.toml for how that Worker itself is configured.
VITE_API_BASE_URL=https://pinky-share-api.<you>.workers.dev
